using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class resetTaskNum : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        VariableManager.taskNum = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
